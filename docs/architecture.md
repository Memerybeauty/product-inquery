# 架构说明

## 顶层架构

```
┌────────────────────────────────────────────────────────────┐
│  ① 客户端                                                  │
│  企业微信用户 / WorkTool 安卓机器人 / 企业微信官方客户端      │
└────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────┐
│  ② 公网 / 外部                                              │
│  WorkTool 云平台 / LLM (Qwen / Sonnet) / 联网搜索          │
└────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────┐
│  ③ 服务器入口（生产环境 192.168.1.236）                      │
│  Nginx 统一网关                                              │
└────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────┐
│  ④ Agent 应用层（本仓库）                                   │
│  DeepAgent v3 · FastAPI · LangGraph 编排                    │
│  6 件套合规中间件 · 11 个 tool                                │
└────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────┐
│  ⑤ 数据 / 知识层                                            │
│  PostgreSQL / RAGFlow / MinIO / MCP                         │
└────────────────────────────────────────────────────────────┘
```

## Agent 内部架构

### A. 三路径决策

```
用户消息
   │
   ▼
route_user_intent  ← DeepAgent 内部决策
   │
   ├── 查问题  → search_qa_kb → search_rag_kb → grade_answer_confidence
   │           → (score<0.7) internet_search → disclaimer → 输出
   │
   ├── 查资料  → list_file_catalog → fetch_file_from_minio → 输出
   │
   └── 其他    → clarify_user / reject_request / reply_capability
```

### B. 合规中间件链

所有响应必经 6 件套中间件，模型无法绕过：

1. `validate_text_response` — 越界 / 空响应检查
2. `validate_asset_response` — 资产白名单 + 否定识别
3. `strip_source_block` — 剥离「资料依据」区块
4. `forbid_model_common_sense` — 禁止常识补全产品事实
5. `force_direct_qa_bypass` — QA 命中直返
6. `append_internet_disclaimer` — 联网免责声明

## 关键设计

- **三路径不写死**：DeepAgent 通过 system_prompt 自主判断三路径之一
- **文件不改写**：MinIO 字节不经过 LLM，直附响应
- **合规收口**：所有响应必经中间件链，单元测试 + 集成测试双重保障
- **置信度评分**：Doc=0.7 / 联网=0.6，低于阈值触发兜底

## 数据流

| 输入 | 处理 | 输出 |
|---|---|---|
| 用户消息 | 路由 + 工具调用 + 中间件链 | 文本响应 / 文件字节 / 澄清 / 拒绝 |

## 部署

- 本地开发：uv + pytest
- 生产：Docker 容器，部署在 192.168.1.236
- 灰度：per-channel 5% → 20% → 100%
